import { ICheckboxStyles } from './Checkbox.Props';
export interface ICheckboxClassNames {
    root: string;
    label: string;
    checkbox: string;
    checkmark: string;
    text: string;
}
export declare const getClassNames: (styles: ICheckboxStyles, className: string, disabled: boolean, isChecked: boolean, isReversed: boolean) => ICheckboxClassNames;
