/// <reference types="react" />
import * as React from 'react';
export interface ISplitDropDownButtonState {
    isContextMenuShown: boolean;
}
export declare class CommandBarCustomizationExample extends React.Component<{}, ISplitDropDownButtonState> {
    private container;
    constructor(props: {});
    render(): JSX.Element;
    private _renderSplitButtonMenuItem(item);
    private _onClickChevron(ev);
    private _toggleDropDownMenuShown(ev);
}
