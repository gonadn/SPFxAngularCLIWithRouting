import Package from './Package';
export default class PackageLookup {
    private _packageMap;
    constructor();
    loadTree(root: Package): void;
    getPackage(nameAndVersion: string): Package | undefined;
}
