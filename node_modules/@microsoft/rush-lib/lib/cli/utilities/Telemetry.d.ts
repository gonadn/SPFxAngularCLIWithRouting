import RushConfiguration from '../../data/RushConfiguration';
export interface ITelemetryData {
    name: string;
    duration: number;
    result: string;
    timestamp?: number;
    platform?: string;
    rushVersion?: string;
    extraData?: {
        [key: string]: string;
    };
}
export default class Telemetry {
    private _rushConfiguration;
    private _enabled;
    private _store;
    private _dataFolder;
    constructor(_rushConfiguration: RushConfiguration);
    log(telemetryData: ITelemetryData): void;
    flush(writeFile?: (file: string, data: string) => void): void;
    readonly store: ITelemetryData[];
    /**
     * When there are too many log files, delete the old ones.
     */
    private _cleanUp();
    private _getFilePath();
}
