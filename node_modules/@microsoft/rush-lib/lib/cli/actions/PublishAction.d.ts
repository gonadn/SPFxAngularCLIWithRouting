import RushCommandLineParser from './RushCommandLineParser';
import { BaseRushAction } from './BaseRushAction';
export default class PublishAction extends BaseRushAction {
    private _addCommitDetails;
    private _apply;
    private _includeAll;
    private _npmAuthToken;
    private _npmTag;
    private _parser;
    private _publish;
    private _regenerateChangelogs;
    private _registryUrl;
    private _targetBranch;
    private _prereleaseName;
    private _suffix;
    private _force;
    private _prereleaseToken;
    private _versionPolicy;
    constructor(parser: RushCommandLineParser);
    protected onDefineParameters(): void;
    /**
     * Executes the publish action, which will read change request files, apply changes to package.jsons,
     */
    protected run(): void;
    private _publishChanges(allPackages);
    private _publishAll(allPackages);
    private _gitAddTags(git, orderedChanges);
    private _npmPublish(packageName, packagePath);
    private _packageExists(packageConfig);
}
