"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
Object.defineProperty(exports, "__esModule", { value: true });
const gulp_core_build_1 = require("@microsoft/gulp-core-build");
class TextTask extends gulp_core_build_1.GulpTask {
    constructor() {
        super('text', {
            textMatch: ['src/**/*.txt']
        });
    }
    executeTask(gulp) {
        /* tslint:disable:typedef */
        const merge = require('merge2');
        const texttojs = require('gulp-texttojs');
        const { textMatch } = this.taskConfig;
        const { libFolder, libAMDFolder } = this.buildConfig;
        /* tslint:enable:typedef */
        if (textMatch) {
            const commonJSTextStream = gulp.src(textMatch)
                .pipe(texttojs({
                template: 'module.exports = <%= content %>;'
            }))
                .pipe(gulp.dest(libFolder));
            if (libAMDFolder) {
                return merge(commonJSTextStream, gulp.src(textMatch)
                    .pipe(texttojs())
                    .pipe(gulp.dest(libAMDFolder)));
            }
            else {
                return commonJSTextStream;
            }
        }
    }
}
exports.TextTask = TextTask;

//# sourceMappingURL=TextTask.js.map
