import IFile from '../models/packageXml/IFile';
/** Loads a directory of files into IFile objects */
export declare function getFiles(cwd: string, globs: string, foundMessage?: string): Promise<IFile[]>;
/** Reads the list of relative path from a specified folder */
export declare function getRelativeFilePaths(cwd: string, globs: string, foundMessage?: string): Promise<string[]>;
