/**
 * Internal constants used for the Graph Http Client
 * PBI 413231 - Remove hard-coded graph endpoint URL from GraphHttpClient.
 * @internal
 */
export default class GraphConstants {
    static readonly PROD_OAUTH_TOKEN_URL: string;
    static readonly EDOG_OAUTH_TOKEN_URL: string;
}
